/**
 * @mmpi2/db
 *
 * Prisma schema, generated client, migration helpers, and seed utilities.
 *
 * Usage:
 *   import { prisma } from '@mmpi2/db/client';
 *   import type { User, ExamSession } from '@mmpi2/db';
 */

export type { PrismaClient, Prisma } from './generated/client/index.js';
